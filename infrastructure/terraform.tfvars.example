# Example terraform.tfvars file
# Copy this to terraform.tfvars and fill in your values

# Required variables
repository_name = "your-github-username/summonsscraper"

# Optional overrides
environment          = "dev"
aws_region          = "us-east-1"
ec2_instance_type   = "t3.small"
key_pair_name       = "your-key-pair-name"

# Cost optimization options
enable_spot_instance    = false              # Enable for up to 90% savings (but can be interrupted)
auto_shutdown_enabled   = true               # Auto shutdown during off hours
shutdown_schedule      = "0 22 * * ? *"      # 10 PM UTC daily (EventBridge cron format)
startup_schedule       = "0 8 ? * MON-FRI *" # 8 AM UTC, Monday-Friday (EventBridge cron format)

# Security - restrict access to your IP
# allowed_cidr_blocks = ["your.ip.address/32"]aform.tfvars file
# Copy this to terraform.tfvars and fill in your values

# Required variables
repository_name = "maxo99/summonsscraper"

# Optional overrides
environment          = "dev"
aws_region          = "us-east-1"
ec2_instance_type   = "t3.small"
key_pair_name       = "your-key-pair-name"

# Security - restrict access to your IP
# allowed_cidr_blocks = ["your.ip.address/32"]
